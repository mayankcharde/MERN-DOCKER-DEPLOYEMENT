# NODE KE VERSION KO INTIALIZE KRTE HAI
FROM node:22-alpine

#  EK NEW DIRECTORY BANATE HAI APP NAAM SE 
WORKDIR /app

#  COPY KREGE SARE PACKAGES SERVER(BACKEND) KE CURRENT DIRECTORY SE
COPY package*.json ./

# NODE MODULES KO INSTALL KREGA 
RUN npm install

# COPY KREGA CUURENT DIR SE APP(MAIN) DIR ME 
COPY . .

# SERVER(BACKEND) APNA 4000 ME RUN KREGA EXPOSE KR DEGE
EXPOSE 4000

#  COMMANDS FOR STARTING THE SERVER(BACKEND)
CMD [ "npm", "start" ]
